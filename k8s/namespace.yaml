apiVersion: v1
kind: Namespace
metadata:
  name: bitcoin-app
  labels:
    # 🛡️ Pod Security Standards - רמת ביטחון גבוהה
    pod-security.kubernetes.io/enforce: restricted
    pod-security.kubernetes.io/audit: restricted  
    pod-security.kubernetes.io/warn: restricted
---
# 🎯 Resource Limits למניעת DoS
apiVersion: v1
kind: LimitRange
metadata:
  name: resource-limits
  namespace: bitcoin-app
spec:
  limits:
  - default:
      cpu: "200m"
      memory: "256Mi"
    defaultRequest:
      cpu: "100m"  
      memory: "128Mi"
    max:
      cpu: "500m"
      memory: "512Mi"
    min:
      cpu: "50m"
      memory: "64Mi"
    type: Container
---
# 🔒 ServiceAccount עם הרשאות מינימליות
apiVersion: v1
kind: ServiceAccount
metadata:
  name: bitcoin-app-sa
  namespace: bitcoin-app