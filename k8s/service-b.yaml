apiVersion: v1
kind: Service
metadata:
  name: service-b
  namespace: bitcoin-app  # 🎯 רץ בnamespace מאובטח
  labels:
    app: service-b
spec:
  selector:
    app: service-b
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8080
  type: ClusterIP
